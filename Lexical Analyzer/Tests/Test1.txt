const m = 7, n = 85;
var  i,x,y,z,q,r;
procedure mult; //hi im a comment
  var a, b;
  begin
     a := x;  b := y; z := 0;
     while b > 0 do
     begin
        if odd x then z := z+a;
           a := 2*a; //im also a comment
           b := b/2;
     end
  end;

begin
  x := m;//
  y := n;
  call mult;
end. //comment!
